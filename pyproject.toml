[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "umriss"
version = "0.1.0"
description = "Agent-facing CLI for building digital twins from reported survey marginals."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "John Joseph Horton" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Scientific/Engineering",
]
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
  "Jinja2>=3.1",
  "PyYAML>=6.0",
  "scipy>=1.10",
]

[project.optional-dependencies]
edsl = ["edsl @ git+https://github.com/expectedparrot/edsl.git@eac96cba21c10cd75ccb5de58073f96599efa3ba"]
test = ["pytest>=8.0"]
dev = ["pytest>=8.0", "ruff>=0.6"]

[project.scripts]
umriss = "umriss.cli:main"

[tool.setuptools.packages.find]
include = ["umriss*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
